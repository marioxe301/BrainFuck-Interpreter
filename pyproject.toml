[project]
name = "brainfuck-interpreter"
version = "0.1.0"
description = "Brainfuck parser/interpreter with a simple web runner"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pyparsing>=3.2.0",
]

[tool.uv]
package = false
